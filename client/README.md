
# ReminderApp Client (React + Vite)

## Prerequisites
- Node.js 18+

## Run (dev)
```bash
npm install
npm run dev
```
Open `http://localhost:5173`.

The dev server proxies `/api` to `http://localhost:5080`.
